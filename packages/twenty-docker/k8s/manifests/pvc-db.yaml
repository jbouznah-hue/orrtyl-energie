apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orrtyl-crm-db-pvc
  namespace: orrtyl-crm
spec:
  storageClassName: default
  volumeName: orrtyl-crm-db-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
