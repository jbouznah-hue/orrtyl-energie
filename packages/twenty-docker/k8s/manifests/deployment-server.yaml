apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: orrtyl-crm-server
  name: orrtyl-crm-server
  namespace: orrtyl-crm
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: orrtyl-crm-server
  template:
    metadata:
      labels:
        app: orrtyl-crm-server
    spec:
      volumes:
        - name: orrtyl-crm-server-data
          persistentVolumeClaim:
            claimName: orrtyl-crm-server-pvc
        - name: orrtyl-crm-docker-data
          persistentVolumeClaim:
            claimName: orrtyl-crm-docker-data-pvc
      containers:
        - name: orrtyl-crm
          image: orrtyl/twenty:latest
          imagePullPolicy: Always
          env:
            - name: NODE_PORT
              value: 3000
            - name: SERVER_URL
              value: "https://crm.example.com:443"
            - name: "PG_DATABASE_URL"
              value: "postgres://postgres:postgres@orrtyl-crm-db.orrtyl-crm.svc.cluster.local/default"
            - name: "REDIS_URL"
              value: "redis://orrtyl-crm-redis.orrtyl-crm.svc.cluster.local:6379"
            - name: SIGN_IN_PREFILLED
              value: "false"
            - name: STORAGE_TYPE
              value: "local"
            - name: "ACCESS_TOKEN_EXPIRES_IN"
              value: "7d"
            - name: "LOGIN_TOKEN_EXPIRES_IN"
              value: "1h"
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: tokens
                  key: accessToken
          ports:
            - containerPort: 3000
              name: http-tcp
              protocol: TCP
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1024Mi"
              cpu: "1000m"
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /app/docker-data
              name: orrtyl-crm-docker-data
            - mountPath: /app/packages/twenty-server/.local-storage
              name: orrtyl-crm-server-data
      dnsPolicy: ClusterFirst
      restartPolicy: Always
